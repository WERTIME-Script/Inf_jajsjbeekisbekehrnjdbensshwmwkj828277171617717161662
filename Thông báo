local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game.CoreGui
ScreenGui.Name = "SharpNotify"

local MainFrame = Instance.new("Frame")
MainFrame.Name = "Main"
-- Kích thước nhỏ lại (Rộng 260, Cao 80)
MainFrame.Size = UDim2.new(0, 260, 0, 80)
-- Đẩy lên trên (Y = 0.6 thay vì 0.8) và xuất phát từ ngoài màn hình
MainFrame.Position = UDim2.new(1, 50, 0.6, 0) 
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.BackgroundTransparency = 0.45
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

-- Bo góc & Viền Neon sắc nét
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 10)
Corner.Parent = MainFrame

local Stroke = Instance.new("UIStroke")
Stroke.Thickness = 2
Stroke.Color = Color3.fromRGB(0, 255, 255)
Stroke.Parent = MainFrame

-- Hiệu ứng kính mờ chéo 45 độ
local Gradient = Instance.new("UIGradient")
Gradient.Rotation = 45
Gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
})
Gradient.Parent = MainFrame

-- Tên ở trên
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -45, 0, 25)
Title.Position = UDim2.new(0, 12, 0, 8)
Title.Text = "ModX"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

-- Lời muốn gửi ở dưới
local Content = Instance.new("TextLabel")
Content.Size = UDim2.new(1, -24, 0, 35)
Content.Position = UDim2.new(0, 12, 0, 35)
Content.Text = "Thank you for using the ModX script Enjoy using it..."
Content.TextColor3 = Color3.fromRGB(220, 220, 220)
Content.Font = Enum.Font.Gotham
Content.TextSize = 13
Content.BackgroundTransparency = 1
Content.TextWrapped = true
Content.TextXAlignment = Enum.TextXAlignment.Left
Content.Parent = MainFrame

-- Nút X chui vào ngay lập tức
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Position = UDim2.new(1, -28, 0, 8)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 18
CloseBtn.Parent = MainFrame

-- Draggable logic (Kéo thả UI)
local dragging, dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)
MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
end)
RunService.RenderStepped:Connect(function()
    if dragging and dragInput then
        local delta = dragInput.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
MainFrame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)

-- Animation Chui Ra / Chui Vào
local targetPos = UDim2.new(1, -280, 0.6, 0)
local hidePos = UDim2.new(1, 50, 0.6, 0)
local tweenInfoOut = TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
local tweenInfoIn = TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.In)

local function HideAndDestroy()
    local tw = TweenService:Create(MainFrame, tweenInfoIn, {Position = hidePos})
    tw:Play()
    tw.Completed:Connect(function() ScreenGui:Destroy() end)
end

CloseBtn.MouseButton1Click:Connect(HideAndDestroy)

-- Start
local show = TweenService:Create(MainFrame, tweenInfoOut, {Position = targetPos})
show:Play()

task.delay(4, function()
    if MainFrame and MainFrame.Parent then
        HideAndDestroy()
    end
end)
